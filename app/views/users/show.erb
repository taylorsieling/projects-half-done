<h2>Welcome, <%= "#{@user.username}" %>!<h2>

<h3>Your Projects:</h3>

<div>
<% @user.projects.each do |project|%>
    <h4><%= "#{project.name}"%></h4>
    <p>Category: <%= "#{project.category}"%><br>
    Knit Time: <%= "#{project.knit_time}"%> hours</p>
<% end %>
</div>


